<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ParentThemeLayoutInputs">
        <item name="android:background">#8A0707</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">30px</item>
        <item name="android:text">#ffffff</item>

    </style>

    <style name="donor_registration">
        <item name="android:textSize">50px</item>
    </style>

</resources>
